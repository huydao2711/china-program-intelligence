[build]
builder = "NIXPACKS"

[deploy]
# ONE-SHOT: crawl all 67 sources now, then exit.
# After crawl is done, this will be restored to daily cron mode.
startCommand = "python main.py --crawl"
restartPolicyType = "NEVER"
